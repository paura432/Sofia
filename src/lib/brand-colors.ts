/** Mirrors `:root` tokens in globals.css for contexts without CSS variables (OG image, viewport). */
export const brandColors = {
  background: "#f6f1e8",
  foreground: "#11110f",
  muted: "#625c54",
  accent: "#a52522",
} as const;
